pipeline {
        agent { docker { 'alpine' } }
        stages {
            stage('build') {
                environment {
                  HOME="."
                }
                steps {
                    sh 'node --version'
                }
           }
        }
    }
